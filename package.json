{
  "name": "kahr",
  "private": true,
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev -w backend",
    "build": "npm run build -w backend",
    "preview": "npm run preview -w backend",
    "deploy": "npm run deploy -w backend",
    "cf-typegen": "npm run cf-typegen -w backend"
  }
}
